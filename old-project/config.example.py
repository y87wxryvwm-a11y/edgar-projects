# config.example.py — copy this file to config.py and fill in your details.
# config.py is gitignored and never pushed to the repo.
#
# The SEC requires a valid name and contact email in every request's
# User-Agent header per their access guidelines:
# https://www.sec.gov/os/accessing-edgar-data

USER_AGENT = "Your Name your.email@example.com"
