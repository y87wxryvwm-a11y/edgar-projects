# Data files, not code (CSV/Excel outputs and inputs)
*.csv
*.xlsx

# Project instructions — local only
AGENTS.md
CLAUDE.md

# Python cache
__pycache__/
*.pyc

# Virtual environments and local EDGAR caches (large, machine-specific)
.venv/
**/.cache/

# Local data output folders (DATA_DIR may point inside the repo on some machines);
# only code is committed, never fetched filings / evidence / results
shares-outstanding/data/
shares-outstanding-census/data/
registrant-count/data/
registrant-count/verify_workflow.js
registrant-dashboard/data/
registrant-dashboard/node_modules/
registrant-dashboard/.verify/
registrant-dashboard/package-lock.json
registrant-dashboard/package.json
# macOS-specific launcher — local only, never pushed (repo ships the Windows .bat)
registrant-dashboard/*.command
proxy/company_size_charts/
proxy/topic_trends/

# macOS
.DS_Store

# Local config — contains personal User-Agent details, never commit
config.py

# Environment / secrets — never commit these
.env
*.token
