# Copy to config.py (git-ignored) and set this once on each machine.
# Keep downloaded archives and generated evidence outside the source-code folder.
DATA_DIR = r"/path/to/your/tlac-data"
USER_AGENT = "Your Name your.email@example.com"
